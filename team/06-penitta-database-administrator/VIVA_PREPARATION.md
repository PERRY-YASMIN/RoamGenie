# Viva Preparation

Be ready to explain: your role; one owned file; its input/output; one validation rule; one failed test you fixed; one dependency; one security control; and how your work reaches PostgreSQL/API/UI without bypassing layers.

Role question: Why is **PostgreSQL setup, databases/roles, least privilege, migration runs, backups/restores, index checks and security** separated from other modules? Demonstrate with version, role privileges, migration log, backup file metadata, restored row counts, EXPLAIN results.
