# Viva Preparation

Be ready to explain: your role; one owned file; its input/output; one validation rule; one failed test you fixed; one dependency; one security control; and how your work reaches PostgreSQL/API/UI without bypassing layers.

Role question: Why is **PostgreSQL DDL/DML, sample data, 15+ queries, joins, views, routines, purposeful triggers, transactions, indexes, reports** separated from other modules? Demonstrate with clean execution log, query outputs, constraint failures, transaction rollback, EXPLAIN output.
