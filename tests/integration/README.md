# Integration Tests

M2 starts auth/PostgreSQL integration here. M4 must automate register → login → trip → generate → budget → save → retrieve → edit/delete → logout. Do not label mock-only repository tests as PostgreSQL integration.
