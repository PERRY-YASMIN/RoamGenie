# Backend Tests

Add authentication, ownership, CRUD, validation, error-envelope, budget and transaction tests here as modules are implemented. The executable starter tests are in `backend/tests/test_api.py`.

